# nginx reverse proxy in front of gnc-backend.
# Local-LAN only per v5.4 §1.6 (no public-internet exposure).

events {}

http {
    upstream gnc_backend {
        server gnc-backend:8080;
    }

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    server {
        listen 80;
        server_name _;

        # REST API
        location /api/ {
            proxy_pass         http://gnc_backend;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_read_timeout 60s;
        }

        # WebSocket
        location /ws/ {
            proxy_pass         http://gnc_backend;
            proxy_http_version 1.1;
            proxy_set_header   Upgrade           $http_upgrade;
            proxy_set_header   Connection        $connection_upgrade;
            proxy_set_header   Host              $host;
            proxy_read_timeout 3600s;
        }

        # Health
        location = /healthz {
            proxy_pass http://gnc_backend/api/v1/health;
        }
    }
}
